{
  "name": "kirillbobyrev.com",
  "private": true,
  "scripts": {
    "format": "prettier --write \"layouts/**/*.html\"",
    "format:check": "prettier --check \"layouts/**/*.html\"",
    "check-links": "hugo --gc --minify && lychee --offline --no-progress --root-dir ./public ./public"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.30",
    "prettier": "^3.4.2",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
