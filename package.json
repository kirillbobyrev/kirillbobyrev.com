{
  "name": "kirillbobyrev.com",
  "private": true,
  "scripts": {
    "format": "prettier --write \"themes/*/layouts/**/*.html\"",
    "format:check": "prettier --check \"themes/*/layouts/**/*.html\"",
    "check-links": "hugo --gc --minify && lychee --offline --no-progress --root-dir ./public ./public"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.78",
    "prettier": "^3.9.4",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
